makeAdder = [base::Int ->
    add1 = [x::Int ->
        add2 = [y::Int ->
            x + y + base
        ]
        add2
    ]
    add1
]

makeAdder = [base::Int ->
    add1 = [x::Int ->
        add2 = [y::Int ->
            x + y + base
        ]
        add2
    ]
    add1
]
>? makeAdder

makeAdder base: 10 |> x: 10 |> y: 20 |> echo

