IrModule toJson -> String = [
  TO DO: "IR JSON serialization is not implemented yet"
]

IrModule toDebugString -> String = [
  ^ "IrModule " + pkg + " (functions: " + (functions count toString) + ", types: " + (types count toString) + ")"
]
