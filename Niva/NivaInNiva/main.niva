type Program
  io: IO_Container


type FilePath = String
type FileContent = String

type NivaSourceFile
  path: String
  nameWithoutExt: String

type IO_Container
  readFile:   [FilePath -> String]
  writeFile:  [FilePath, FileContent -> Unit]
  walkDir:    [FilePath -> List(NivaSourceFile)]
  getParent:  [String -> String]
  currentDir: String
  runProgram: [List(String) -> ]
  runProgramFromPath: [String, List(String) -> ]


constructor IO_Container new -> IO_Container = [
  readFile::[FilePath -> String] = [path::String ->
    FileSystem read: (path toPath)
  ]

  writeFile = [path: String, fileContent: FileContent ->
    FileSystem write: path toPath
               content: fileContent
    ()
  ]

  walkDir::[FilePath -> List(NivaSourceFile)] = [path: String ->
    x = FileSystem listRecursively: path toPath

    x map: [
      NivaSourceFile path: it toString nameWithoutExt: (it name substringBeforeLast: ".")
    ]
  ]

  getParent = [path: String -> path toPath parent unpackOrPANIC toString]

  runProgram = [args: List(String) ->
    processBuilder = ProcessBuilder command: args
    processBuilder redirectOutput start waitFor
    Unit
  ]

  runProgramFromPath = [path: String, args: List(String) ->
    processBuilder = ProcessBuilder command: args dir: path
    processBuilder redirectOutput start waitFor
    Unit

  ]

  ^ IO_Container :readFile :writeFile :walkDir :getParent currentDir: "." :runProgram :runProgramFromPath
]

compilerTry = [
  path = Compiler cliArgs //"~/Documents/Fun/niva_demo/main.niva"
  NivaCompiler runArgs: path withIO: IO_Container new
]

compilerTry2 = [
  //path = {"run" ".niva_js/testss.niva"} //"~/Documents/Fun/niva_demo/main.niva"
  path = {"run" ".niva_go/testss.niva" "--backend" "go"} //"~/Documents/Fun/niva_demo/main.niva"
  t1 = System currentTimeMillis
  NivaCompiler runArgs: path withIO: IO_Container new measureTime: true
  diff = System currentTimeMillis - t1
  //diff echo // 60 toLong / 1000 toLong, echo
]

goErrorDomainHandledMatchCodegen = [
  fileMain = """
    errordomain MyError =
    | Error1 text: String
    | Error2 code: Int

    Unit foo -> Int!{Error1 Error2} = [
      (Error2 code: 404) throw
      ^ 42
    ]

    Unit bar -> Int = [
      default = | () foo
                | Error1 => [0]
                | Error2 => [1]
                |=> [it]
      ^ default
    ]
  """

  result = ResolverHelper resolve: #{"main" fileMain} entryPoint: "main"
  go = GoBackend fullFromResolver: result
  go echo

  go contains: "func main__core_Unit__bar(this struct{}) int {", assertTrue
  go contains: "__matchValue, __matchErr := main__core_Unit__foo(core.UnitInstance)", assertTrue
  go contains: "switch __matchErr.(type) {", assertTrue
  go contains: "case main_Error1:", assertTrue
  go contains: "it := __matchErr.(main_Error1)", assertTrue
  go contains: "it := __matchValue", assertTrue
  Assert that: (go contains: "func main__core_Unit__bar(this struct{}) (int, error)") equals: false

] do
